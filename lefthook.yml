# Runs the function CI runs, so a green hook means a green pipeline. Dagger has
# no host filesystem access, so this checks rather than fixes: autofix is
# `dagger call format export --path=.`.
pre-commit:
  commands:
    check:
      run: dagger call check
