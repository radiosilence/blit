{
  "scripts": {
    "build": "(cd web && astro build && docker build -t antihero/blitcc .)",
    "push": "yarn build && docker push antihero/blitcc"
  },
  "workspaces": [
    "web"
  ],
  "private": true
}
