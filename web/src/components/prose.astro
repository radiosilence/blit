---
import cx from "classnames";

interface Props {
  class?: string;
}

const { class: cls = "", ...rest } = Astro.props;
---

<article class={cx("prose", "prose-sm", "max-w-screen-lg", cls)} {...rest}>
  <slot />
</article>
